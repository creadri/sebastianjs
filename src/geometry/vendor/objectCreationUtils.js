// Vendored from svgdom@0.1.28 (src/utils/objectCreationUtils.js) — MIT.
// Copyright (c) 2017-2023 Ulrich-Matthias Schäfer. See src/geometry/vendor/LICENSE.
// Generated by scripts/vendor-svgdom.mjs — do not edit by hand.
export const extend = (...modules) => {
  var methods, key, i

  // Get object with extensions
  methods = modules.pop()

  for (i = modules.length - 1; i >= 0; i--) {
    for (key in methods) {
      modules[i].prototype[key] = methods[key]
    }
  }
}

export const extendStatic = (...modules) => {
  var methods, key, i

  // Get object with extensions
  methods = modules.pop()

  for (i = modules.length - 1; i >= 0; i--) {
    for (key in methods) {
      modules[i][key] = methods[key]
    }
  }
}

// TODO: refactor so that it takes a class
export const mixin = (mixin, _class) => {
  const descriptors = Object.getOwnPropertyDescriptors(mixin)

  Object.defineProperties(_class.prototype, descriptors)
}
